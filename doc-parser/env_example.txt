# Vision配置
VLM_API_KEY=your-vlm-api-key-here
VLM_API_BASE=https://open.bigmodel.cn/api/paas/v4
VLM_MODEL=glm-4v-plus-0111
VLM_TEMPERATURE=0.8
VLM_TOP_P=0.6
VLM_MAX_TOKENS=1024

# Vision提示词配置
EXCEL_VISION_PROMPT=请仔细分析这张图片中的所有内容，包括所有表格和子图。要求：1. 完整提取所有可见的文字信息，包括表格内容、图表标题、图例等 2. 保持原有的层级结构和组织方式 3. 对于表格数据，确保表头和对应的数据正确对齐 4. 对于子图，提取其标题、说明文字和相关数据 5. 使用嵌套的 JSON 结构来组织信息 请确保输出的 JSON 格式正确，所有属性名和属性值都正确对应。
IMAGE_PROMPT=请根据图片内容生成一段对图片的描述
TABLE_PROMPT=你是一个多模态智能助手，具备理解图像和自然语言的能力。现在请你从以下表格图片中，准确提取结构化信息。 请注意表格可能包含以下特征： 多级表头或合并单元格； 不规则排版或图文混排； 含有备注、脚注、颜色标记等附加信息； 任务要求： 识别表格中的所有字段及其层级结构（如存在多级表头，请按层级展示）。 按照表格的原始逻辑，提取出每一行/列的关键信息，转为标准结构化格式（如JSON或Markdown表格）。 若表格中有图文混排、符号标记、特殊颜色提示，请予以解释。 若有备注或脚注内容，请单独提取归类。 输出内容要求精准、简洁，保持表格原意。 请开始分析这张图片中的表格信息。

# MinIO存储配置
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin
MINIO_HOST=127.0.0.1:9000
MINIO_PREFIX=http://127.0.0.1:9000/

# S3协议存储配置
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_REGION=cn-north-1
S3_ENDPOINT=127.0.0.1:9000
S3_PREFIX=http://127.0.0.1:9000/

# TOS对象存储配置
TOS_AK=your-tos-access-key
TOS_SK=your-tos-secret-key
TOS_REGION=cn-beijing
TOS_ENDPOINT=tos-cn-beijing.ivolces.com
TOS_PUBLIC_ENDPOINT=tos-cn-beijing.volces.com
TOS_BUCKET=knowledge-center-dev
TOS_TEMP_OBJECT_KEY_PREFIX=shark
OBJECT_STORAGE_SIGNATURE_VERSION=v4

# 服务相关参数
SERVICE_ENVIRONMENT=ONLINE
SERVICE_USE_STORAGE=TOS
SERVICE_MAX_JOB_NUMBER=32
SERVICE_TIME_OUT=14400
SERVICE_IMG_BASE64_PREFIX=data:image/png;base64,
SERVICE_DEFAULT_BUCKET=my-new-bucket
SERVICE_CDN_HOST=http://localhost:6006/oss/endpoint/put-file
SERVICE_CALL_BACK_URL=http://localhost:7100/loader/batch_distill/callback

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# RabbitMQ配置
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/
RABBITMQ_TASK_EXCHANGE=task.exchange
RABBITMQ_TASK_QUEUE=dataParsingQueue
RABBITMQ_TASK_ROUTING_KEY=task.dataParsing
RABBITMQ_RESULT_EXCHANGE=task.exchange
RABBITMQ_RESULT_QUEUE=taskActionQueue
RABBITMQ_RESULT_ROUTING_KEY=task.action
RABBITMQ_PREFETCH_COUNT=1
RABBITMQ_HEARTBEAT=600
RABBITMQ_BLOCKED_TIMEOUT=300

# 是否启用RabbitMQ消费者进程
ENABLE_RABBITMQ_CONSUMER=false